# Bootstrap prompt — Drop-in Agent Bundle Adoption

**Version:** 1.0  
**Updated:** 2026-09-09T11:30:00Z

```text
Adopt the Agentic Flow framework that has been placed/extracted into this repository.

Do not start product mutation yet.

1. Read `docs/agent/START_HERE.md` from the framework bundle.
2. Detect whether this repository is NEW-ADOPTION or MIDSTREAM-ADOPTION.
3. If any product work already exists in the working tree/session, read `docs/agent/MIDSTREAM_ADOPTION.md` and create the adoption snapshot before further mutation.
4. Read canonical `ARCHITECTURE.md` and `VERSION`.
5. Inspect existing project agent instructions, project/build/test/deploy structure, branch/dirty state, current task, and project-profile equivalents.
6. Generate/update only the minimum vendor adapter for this harness; do not replace working project-specific rules blindly.
7. Create or propose `.agentic/PROJECT_PROFILE.yaml` from `templates/PROJECT_PROFILE.example.yaml` only after inspecting real project requirements. Missing controls are gaps, not invented PASS values.
8. Keep operator documentation and research out of permanent agent context.
9. Validate that a fresh session can discover source of truth, current work, build/test commands, project profile, task workflow, verification router, production router and environment policy.
10. Return an adoption receipt: framework version, mode, changed adapter paths, profile/gaps, conflicts, current-task reconciliation and validation checks.

STOP after adoption/bootstrap validation unless the user separately authorizes product work.
```
