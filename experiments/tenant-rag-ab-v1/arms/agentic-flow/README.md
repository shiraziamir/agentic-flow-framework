# Arm B — `tenant-rag-agentic`

This is the treatment project.

Seed the new implementation repository with the same shared `PROJECT_SPEC.md` and `RUN_RECORD.template.md` (renamed `RUN_RECORD.md`). Make Agentic Flow 1.11 available using the normal adoption mechanism, give the agent `AGENTIC_FLOW_BOOTSTRAP.md`, then release exactly the same Q01..Q12 prompts in the same order.

Use `PRODUCT_STANDARD` and `PRODUCT_BUILD` as the starting posture. The framework may create its normal triggered `.agentic/` state, System Truth Map, contracts, receipts, reviews and role handoffs.

For the first paired trial, prefer the same base model/provider/harness as the baseline. Role separation may use fresh contexts but should not silently introduce a stronger model.

Do not expose `EVALUATOR_ORACLE.md` before freeze. Freeze immediately after Q12 and required closure.
