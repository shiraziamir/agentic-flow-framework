# Arm A — `tenant-rag-baseline`

This is the control project.

Seed the new implementation repository with the shared `PROJECT_SPEC.md` and `RUN_RECORD.template.md` (renamed `RUN_RECORD.md`). Give the implementation agent `BASELINE_BOOTSTRAP.md`, then release Q01..Q12 from the shared queue one at a time.

Do not expose Agentic Flow files, terminology, or `EVALUATOR_ORACLE.md` to this arm before freeze.

The agent is still expected to behave like a competent professional coding agent: tests, refactoring, debugging, documentation and ordinary engineering judgment are allowed. The baseline must not be intentionally weakened.

Freeze immediately after Q12.
