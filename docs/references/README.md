# Reference Index

Start with `PRIMARY_SOURCES.md` for the consolidated list of official/vendor/standards sources used by the framework.

These files are human/reference material and are excluded from the default Agent Bundle. Agents load them only when current external provenance or a research decision requires them.
